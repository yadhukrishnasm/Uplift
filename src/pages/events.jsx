import React from 'react'

export default function Events() {
  return (
    <div>
        <div className="p-10 relative flex flex-col gap-6">
        <span className="text-2xl">Events</span>
        
        <span className="text-base font-bold my-4">you created</span>
        <div className="flex flex-col justify-center items-center">
        {/* {listevent.map((content,index)=>(
             <EventPreview img={content.image} title={content} desc={} time={} slots={}/> 
        ))} */}
        </div>
        <span className="font-bold">participating</span>
    </div>
    </div>
  )
}
